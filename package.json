{
  "dependencies": {
    "@rails/actiontext": "^7.0.4",
    "@tailwindcss/ui": "^0.7.2",
    "alpinejs": "^3.10.3",
    "trix": "^2.0.0-beta.0"
  },
  "scripts": {
    "start": "bundle install && rails server",
    "build": "vue-cli-service build",
    "lint": "vue-cli-service lint"
  }
}
