{
  "dependencies": {
    "@rails/actiontext": "^7.0.4",
    "@tailwindcss/ui": "^0.7.2",
    "alpinejs": "^3.10.3",
    "trix": "^2.0.0-beta.0"
  },
  "scripts": {
    "start": "rails server",
    "install": "apt-get install -y libpq-dev && apt-get install build-essential && apt-get install xz-utils",
    "lint": "vue-cli-service lint"
  }
}
